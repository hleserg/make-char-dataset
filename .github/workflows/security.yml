name: Security

on:
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch:
  pull_request:
    paths:
      - "pyproject.toml"
      - "uv.lock"

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - run: uv sync --all-extras
      - name: Dependency audit (pip-audit)
        # Try the PyPI advisory service; fall back to OSV (osv.dev, a superset DB)
        # if PyPI is unreachable, so the audit is resilient to registry outages.
        run: uv run pip-audit || uv run pip-audit --vulnerability-service osv
      - name: Static security (bandit)
        run: uv run bandit -c pyproject.toml -r src/ -q
